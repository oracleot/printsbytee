{
  "name": "printsbytee",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter @printsbytee/api dev",
    "dev:website": "pnpm --filter website dev",
    "dev:business-app": "pnpm --filter business-app dev"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
