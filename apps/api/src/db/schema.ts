/**
 * Placeholder for the Drizzle schema.
 *
 * The real schema (products, production_batches, batch_items, sales,
 * enquiries, waitlist_entries, users, sessions) lands in issue #10 and
 * must match `docs/data-model.md` exactly. Until then, this file is
 * intentionally empty so the API can boot without a database.
 *
 * Exports are intentionally omitted so adding real tables later is a
 * single, focused change rather than a cleanup over a stub.
 */
export {};